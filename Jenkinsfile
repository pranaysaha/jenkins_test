pipeline {
    agent {
        label 'Lubuntu_VM'
    }
    stages {
        stage('build') {
            steps{
                sh 'ls'
                sh 'docker images'
            } 
        }
    }
}
